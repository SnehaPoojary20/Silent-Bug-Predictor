from github import Github
import pandas as pd
import os

token = os.getenv("GITHUB_TOKEN")

g = Github(token)

repo = g.get_repo("tensorflow/tensorflow")

commits = repo.get_commits()

data = []

bug_keywords = ["fix", "bug", "error", "issue"]

for commit in commits[:300]:

    message = commit.commit.message.lower()

    is_buggy = any(word in message for word in bug_keywords)

    files = commit.files

    if files:

        for file in files:

            data.append({
                "file_name": file.filename,
                "commit_churn": file.changes,
                "file_depth": file.filename.count("/"),
                "buggy": int(is_buggy)
            })

# Create dataframe
df = pd.DataFrame(data)

# Save CSV
df.to_csv("training_data.csv", index=False)

print("Dataset created successfully")